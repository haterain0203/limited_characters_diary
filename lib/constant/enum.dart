enum InputDiaryType {
  add,
  update,
}

enum NotificationDialogTrigger {
  onFirstLaunch,
  userAction,
}

/// Firebase Console の Authentication で設定できるサインイン方法の種別。
enum SignInMethod {
  google,
  apple,
  ;
}

enum SignInType {
  login,
  link,
  ;
}
